[JavaDoc] The API setBackgroundImage() should mention pattern
I think it would be good to mention, that the background-image set via "setBackgroundImage()" is applied as Pattern if not fitting the controls size.
Ben