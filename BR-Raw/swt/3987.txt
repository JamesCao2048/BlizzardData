StyledText - Double-Click-Drag doesn't work, works on Text
A double-click-drag (click, and then press again without releasing) doesn't
work.
This is typically used to swipe-select, but at the word boundary.
This
is what the Text control does.