UNDERLINE_ERROR text style does not work for IME strings
I20071009-0800.
Platform Text now uses the new SWT text styles to draw annotations. While this fixes  bug 201928  while editing the IME string it looks ugly once the string is inserted: the squiggly line is no longer continuous.