Requesting change to AbstractGraphicalEditPart#reorderChild(...)
We've introduced the following changes to our editpart hierarchy /** support multiple content panes per editpart.
*/ IFigure getContentPaneFor( Object key ) { return contentPaneMap.contains(key) ?
(IFigure)contentPaneMap.get(key) : getContentPane() } addChildVisual( EditPart child, int index ) { getContentPaneFor(child).add(...); } removeChildVisual( EditPart child, int index ) { getContentPaneFor(child).remove(...); } However, we are running into problems with AbstractGraphicalEditPart#reord
er