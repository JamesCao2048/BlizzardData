[Trim] Console view does not show up when running an Ant task
M7 When the Conosle view is hidden (by double clicking a file tab in the Java editor, for example) it does not show when running an Ant task.
Note that the Console view shows up if it's closed but the lower docked pane is visible.