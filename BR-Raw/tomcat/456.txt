org.eclipse.swt.widgets.Table.getTopIndex() returns wrong value
The above mentioned function returns wrong a value (0) for an empty table, should return 
-1.Example on an empty table (i.e no elements): 
-------------------------------------------- int index = table.getTopIndex() // !!! returns 0 TableItem item = table.getItem(index); // crashes because there is no such item I came across this bug working with a tableviewer under Win2000: TableViewer viewer = new TableViewer(table); viewer.setInput(
element); where element has no children i.e.
returns an empt