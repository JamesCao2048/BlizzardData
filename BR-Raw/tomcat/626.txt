Project-wide default for Junit Plug-in Test "requires UI"
Currently, when I do "Run as JUnit Plug-in Test", the option on the Arguments
Tab "Plug-in Tests require a user interface" defaults to checked.  I would like
to be able to set this to default off on a project-wide basis.