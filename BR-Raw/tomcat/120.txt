Inspect/Display Context Menu Items disabled until text is selected
The Inspect and Display actions now work without first selecting the text, but the context menu items are still disabled until text is selected.