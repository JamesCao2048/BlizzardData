JDT : report final, report static
Report final use : It is possible with this architecture to report that a member, a method or a parameter or a local variable is used as an final one 
(not reassigned).
In that case a hint would be to mention that.
Report static use : It is possible with this architecture to report that a method is a static one (no access to this).
In that case a hint would be to mention that