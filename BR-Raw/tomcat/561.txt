Paste omits a character
20040119 
- In the following text, double click on 'rel' to get the word selected.

- CTRL + C 
- Set cursor between empty brackets, paste 
-> only pastes 'el', the letter 'r' is gone protected Object getAttribute(Object parent, int childProperty) { if (child instanceof List) { } else if (rel != null && !rel.equals(child)) { } else if () return child; }