Variables view does not persist size of details area
The size of the details area in the variables and expressions view is not persisted. Each time I start my workspace, the details area is opened with the same (default) size, even though I have resized it.