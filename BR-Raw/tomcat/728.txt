Add plugin reference to javadoc
Since some packages (org.eclipse.ui for example) have classes distributed within several plugins (workbench, ide, etc) it would be very useful to have a reference in the javadoc of the classes indicating which plugins provide those classes.