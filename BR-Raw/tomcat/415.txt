document outline focus is different from JDT's
In AJDT the document outline focus switches to the editor if any element is selected. This makes it impossible to up/down arrow through the elements like you can in JDT's outline.