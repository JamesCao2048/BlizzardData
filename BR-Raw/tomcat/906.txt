Variable selection UI
We should make a pass over all launch config fields that support variables and make sure they have some UI for adding variables using the variable selection dialog. For fields where a button isn't good, we should add an item to the context menu "Insert Variable..."