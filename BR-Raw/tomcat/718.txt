Field name should be set when dropping from palette
When dropping a component onto the freeform, there should be some ability to set the field name easily, instead of renaming the field later - which causes a RLFS. Something like a dialog should pop-up when a bean is dropped on the freeform asking for the field name.