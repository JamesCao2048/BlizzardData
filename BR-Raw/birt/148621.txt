Bug 148621 [Regression] Chart is drawn or exception is shown when no rows returned by report engine
Steps: Scenario 1: Create a table Create a table group Insert a chart inside the table group header Use Inherit Data Preview: an empty chart is drawn every time the group has no data Expected: the chart shouldn't be drawn at all Scenario 2: Insert a chart, Bind to dataset using filter to return no rows Empty Chart is drawn Expected: no chart drawn.
Scenario 3: Insert a chart Bind to dataset using filter to return no rows Create a tooltip with an expression Preview: exception is logged at the end of the report Expected: no exception