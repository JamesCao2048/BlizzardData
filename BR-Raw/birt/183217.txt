Bug 183217 suppress duplicates leaving whitespace
I am not certain this is a bug, but if I set suppress duplicates on all the columns, I still get whitespace for suppressed rows.
2.1.2 did not do this.
This issue happens in the M6 build.